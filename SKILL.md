---
name: follow-builders
description: 追踪 X、AI 公司官方博客和 YouTube 播客中的一线 AI 创业者、研究者、产品经理与工程师，并结合最新官方发布与可信市场信息，输出中文 AI 市场快照、Builder 动态、2–3 款带官方链接的优秀 AI 产品推荐和 AI 产品经理行动建议。用于用户调用 /ai、@follow-builders，或要求 AI 行业洞察、Builder 动态、最新 AI 市场情况、前沿产品、AI 工具推荐与产品经理学习方向时。Builder 内容来自中央信息源，不需要用户提供抓取 API Key。
---

# 关注建设者，而非流量人物

追踪真正做产品、经营公司和推进研究的人，提炼他们正在说什么、市场正在发生什么，以及产品经理下一步值得尝试什么。

## 默认规则

- 默认输出简体中文，专业术语与专有名词按中文行业习惯保留英文。
- Builder 动态只使用准备脚本返回的 JSON，不访问 X、播客或文章页面补写内容。
- 市场快照与产品推荐必须联网核验，并优先使用公司官网、官方文档、官方 changelog 和监管机构等一手来源。
- 涉及融资、市场份额、公司经营或监管动态时，可使用 Reuters、FT 等可信独立媒体，并明确来源性质。
- 每个事实性内容块必须有直接链接；没有链接就不收录。
- 明确区分事实、分析推断、推荐意见与未知项。
- 默认推荐 2–3 款产品；不生成随机产品机会卡。
- 日常手动调用不要求设置推送、API Key 或定时任务。

## 调用方式

### 手动生成

用户输入 `/ai`、`@follow-builders` 或直接索要简报时，立即运行完整流程。如果没有配置文件，使用：

```json
{
  "language": "zh",
  "frequency": "on-demand",
  "delivery": { "method": "stdout" }
}
```

不要为了手动简报强制用户完成设置问答。

### 首次介绍

首次使用时，用一句话说明：

“我会把最新 AI 市场变化、一线 Builder 动态和 2–3 款值得体验的 AI 产品整理成一份中文简报，并告诉你这些变化对产品经理意味着什么。”

从 `config/default-sources.json` 读取当前来源数量。只有用户要求查看来源时，才展示完整名单。

### 定时投递

只有用户明确要求每天、每周或在指定时间自动收到简报时，才设置定时投递。此时完整阅读并执行 [定时投递设置](references/scheduled-delivery-zh.md)。在 ChatGPT Work 中优先使用 Automations，不要擅自创建系统 crontab。

## 完整简报流程

### 1. 运行 Builder 准备脚本

先告知用户正在获取最新内容，然后运行：

```bash
cd ${CLAUDE_SKILL_DIR}/scripts && node prepare-digest.js 2>/dev/null
```

脚本输出 JSON，其中包含：

- `config`：语言和投递设置
- `x`：Builder 动态、简介与原始链接
- `blogs`：官方博客文章与原始链接
- `podcasts`：播客标题、文字稿与原始链接
- `prompts`：摘要、市场快照和产品推荐规则
- `stats`：有效内容数量
- `errors`：非致命错误

如果脚本没有输出有效 JSON，说明 Builder 信息源暂时不可用。仍可在明确说明缺失的前提下完成市场快照与产品推荐，但不得凭记忆伪造 Builder 动态。

### 2. 调研最新 AI 市场

完整遵循 `prompts.market_snapshot`：

1. 先检索最近 7 天的 AI 产品发布、模型变化、Agent 进展、商业与监管动态。
2. 7 天内高质量信号不足时扩大到最近 30 天，并标出准确日期。
3. 产品能力、价格、版本与可用性必须从官方页面核验。
4. 融资、公司经营、监管与争议性事件至少使用一个可信独立来源；重大结论尽量交叉核验。
5. 打开来源页面核对正文，不得只根据搜索摘要下结论。
6. 最终只保留 3–4 条会影响产品决策的高信号变化。

市场快照应优先覆盖：

- 模型与 Agent 能力
- 产品形态与用户体验
- 成本、商业化与采用情况
- 安全、版权、合规与平台规则

### 3. 整理 X 动态

逐个处理 `x` 数组：

1. 只用 `bio` 判断人物角色；信息不足时只写姓名。
2. 按 `prompts.summarize_tweets` 提炼原创观点、发布与实践。
3. 每个被采用的观点都附带 JSON 中的 `url`。
4. 跳过闲聊、无评论转发、互动诱饵和纯宣传。

### 4. 整理官方博客

逐篇处理 `blogs` 数组：

1. 使用 JSON 中的博客名、标题、作者和原始链接。
2. 按 `prompts.summarize_blogs` 提炼公告、数字、能力变化和产品影响。
3. 不从标题推断正文没有提供的信息。

### 5. 整理播客

逐集处理 `podcasts` 数组：

1. 只使用 JSON 中的 `name`、`title`、`url` 与 `transcript`。
2. 按 `prompts.summarize_podcast` 提炼具体、反直觉且可迁移的观点。
3. 只链接具体节目页面，不链接频道首页。

### 6. 推荐 2–3 款 AI 产品

完整遵循 `prompts.recommend_products`：

1. 根据用户的目标、能力和使用场景选择产品；没有个人背景时，默认面向 AI 产品经理与内容创作者。
2. 优先选择能解决不同核心任务的产品，避免三款产品功能完全重复。
3. 每款产品名称必须链接到产品官网或官方使用入口。
4. 核验产品当前可用性、适用平台、收费限制与关键能力。
5. 每款说明最适合的任务、推荐理由、局限和一个可立即尝试的测试任务。
6. 推荐不是全市场排名，不得把官方宣传或 benchmark 直接写成客观胜负。

### 7. 组装中文简报

严格按照 `prompts.digest_intro` 组装：

1. 今日先看
2. AI 市场最新情况
3. Builder 动态与产品判断
4. 推荐 2–3 款 AI 产品
5. AI 产品经理行动建议

语言规则：

- `zh`：全部使用简体中文。
- `en`：全部使用英文。
- `bilingual`：每个英文段落后紧跟对应中文，不要先输出整篇英文再输出整篇中文。

翻译时遵循 `prompts.translate`。人名、公司名、产品名与 URL 保持原样。

### 8. 投递

- `stdout`：直接在当前对话输出。
- `telegram` 或 `email`：把最终文本写入临时文件后运行：

```bash
cd ${CLAUDE_SKILL_DIR}/scripts && node deliver.js --file <digest-file> 2>/dev/null
```

投递失败时，把完整简报回退到当前对话。

## 证据与判断规则

- 事实：来源直接明确支持，附直接链接和必要日期。
- 推断：说明推断链条，不冒充来源原意。
- 推荐：说明适用人群和取舍，不使用“最好”“必选”等无条件结论。
- 未知：证据不足、只有单一体验或无法确认时直接标注。
- Builder 动态不得补写 JSON 没有提供的身份、数字、产品发布或背景。
- 引用必须短且准确；无法确认逐字准确时改用转述。
- `errors` 是非致命项；只要 JSON 仍有内容，就使用现有内容完成对应栏目。

## 配置变更

用户可以用自然语言修改：

- “改成英文／中英双语” → 修改 `language`
- “只推荐两个产品” → 每期推荐 2 款
- “重点推荐适合产品经理／开发者／内容创作者的工具” → 调整推荐人群
- “不要市场新闻，只看 Builder” → 跳过市场快照与产品推荐
- “摘要更短／更长” → 修改对应的用户自定义提示模板
- “只在这里发” → 设置 `delivery.method: stdout`

用户要求查看设置时，友好展示当前配置。用户要求新增或移除 Builder 来源时，说明中央来源列表统一维护，可在 https://github.com/jilombmiker-alt/lobe-skills 提交建议。

自定义提示模板应复制到 `~/.follow-builders/prompts/` 后修改，避免中央更新覆盖用户偏好。
